DATABASE_URL="postgresql://saidu:saidu@localhost:5432/uniqualifyer?schema=public"
JWT_SECRET="your-256-bit-encryption-key"  # Minimum 32 chars
JWT_ACCESS_EXPIRES_IN="15m"              # Access token expiry
JWT_REFRESH_EXPIRES_IN="7d"              # Refresh token expiry
JWT_ISSUER="uniqualifyer"               # Token issuer
JWT_AUDIENCE="your-app-domain.com"       # Token audience

# Cookie Settings (if using HTTP-only cookies)
COOKIE_SECURE="false"                     # Set false in development
COOKIE_SAME_SITE="lax"                   # strict|lax|none